import React from "react";

class SaveQuoteButton extends React.Component {
  render() {
    return (
      <button type="button" className="btn btn-primary m-3">
        Save Quote
      </button>
    );
  }
}

export default SaveQuoteButton;
